name: Playwright Tests
on:

  # Cấu hình 1: Tự động chạy khi push code
  push:
    branches: [ main, master ]

  # Cấu hình 2: Tự động chay khi Pull Request - PR từ nhánh phụ vào main
  pull_request:
    branches: [ main, master ]
  
  # Cấu hình 3: Cho phép bấm nút chạy bằng tay
  workflow_dispatch:
    inputs:
      test_scope:
        description: 'Nhập thư mục hoặc Tag cần chạy (Khi bấm bằng tay)'
        type: choice
        required: true
        default: regression-ui
        options:
          - regression-ui
          - regression-api
        
  
jobs:
  test:
    timeout-minutes: 60
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - uses: actions/setup-node@v4
      with:
        node-version: lts/*
    - name: Install dependencies
      run: npm ci
    - name: Install Playwright browsers
      run: npx playwright install --with-deps
    - name: Run Playwright tests RegressionTest_UI
      # Câu lệnh thông minh: Nếu push code sẽ chạy 'tests/', nếu chạy tay sẽ lấy giá trị bạn nhập
      run: npx playwright test --project=${{ github.event.inputs.test_scope || 'regression-ui' }}

    - name: Run Playwright tests RegressionTest_API
      run: npx playwright test --project=${{ github.event.inputs.test_scope || 'regression-api' }}

      env:
        WEB_URL: ${{ secrets.WEB_URL}}
        API_URL: ${{ secrets.API_URL}}
        ADMIN_USER: ${{ secrets.ADMIN_USER}}
        ADMIN_PASS: ${{ secrets.ADMIN_PASS}}
        ACCESS_TOKEN: ${{ secrets.ACCESS_TOKEN}}
    - uses: actions/upload-artifact@v4
      if: always()
      with:
        name: playwright-report
        path: playwright-report/
        retention-days: 30